package core.entity;

// TODO: Analisar a utilidade desse enum, eu quero retornar uma razão de morte
// que seja de acordo mas que o frontend que decida mensagem e etc, então eu retorno na verdade um tipo.
// Ex. { "user": {..., deathReason: "FALL"}}
public enum DeathReason {
    EATEN,
    FALL

}
